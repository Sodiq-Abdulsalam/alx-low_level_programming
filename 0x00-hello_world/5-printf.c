#include <stdio.h>
/**
 * main - this is the entry or start point of the code
 *
 * Return:0 when the main function is successful
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}

#include "main.h"

/**
 * _isupper - function use to check for uppercase character.
 *
 * @c: input character to be checked.
 *
 * Return: return 1 if c if uppercase or 0 otherwise.
 */

int _isupper(int c)
{
	if ((c >= 'A') && (c <= 'Z'))

	return (1);

	else
	return (0);
}
